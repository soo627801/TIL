#include <stdio.h>

int main(void) {
    int n, x, i;
    int a[10000];
    
    scanf("%d %d", &n, &x);
    
    for (i=0; i<n; i++){
        scanf("%d", &a[i]);

        if (a[i] < x)
            printf("%d ", a[i]);
    }
    
    return 0;
}
