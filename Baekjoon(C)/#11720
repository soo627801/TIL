#include <stdio.h>

int main(void) {
    int n, cnt=0;
    char a[101];
    scanf("%d", &n);
    scanf("%s", a);
    
    for (int i=0; i<n; i++) {
        cnt += a[i] - '0';
    }
    printf("%d", cnt);
    
    return 0;
}
