#include <stdio.h>

int main(void) {
    int i, j, max=0;
    int a[9];
    
    for (i=0; i<9; i++){
        scanf("%d", &a[i]);
    }
    
    for (i=0; i<9; i++) {
        if (a[i] >= max) {
            max = a[i];
            j = i; }
    }
    printf("%d\n%d", max, j+1);
    
    return 0;
}
