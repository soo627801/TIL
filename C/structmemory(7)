// 다음 소스 코드를 완성해 인정 정보가 출력되게 만들기 (함수 안의 인수만 채우면 됨)

#define _CRT_SECURE_NO_WARNINGS
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

struct Person {
  char name[20];
  int age;
  char address[100];
};

int main() {
  struct Person *p1 = malloc(sizeof(struct Person));
  struct Person p2;

  strcpy(p1->name, "고길동");
  p1->age = 40;
  strcpy(p1->address, "서울시 서초구 반포동");

  // p1의 내용을 p2로 복사하기
  // memcpy 함수 사용 시, p1은 포인터이므로 그대로 넣고, p2는 구조체 변수이므로 앞에 &를 사용해 메모리 주소를 구해서 넣음.
  // 마지막에는 복사할 크기를 넣어야 하므로 구조체의 크기를 구해서 넣으면 됨.
  memcpy(&p2, p1, sizeof(struct Person));

  printf("이름 : %s\n", p2.name);
  printf("나이 : %d\n", p2.age);
  printf("주소 : %s\n", p2.address);

  free(p1);

  return 0;
}
